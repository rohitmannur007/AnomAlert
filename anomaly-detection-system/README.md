# Advanced Anomaly Detection System — Demo (Gradio)

Run end-to-end anomaly detection (IsolationForest + Autoencoder) with time-series visualization.

## Setup
1. Clone / copy project files.
2. Create and activate Python venv (optional):
   ```bash
   python3 -m venv venv
   source venv/bin/activate
